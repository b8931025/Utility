import { formatToDateTime } from '/@/utils/dateJsUtil';
x.birthday = formatToDateTime(x.birthday, 'YYYY-MM-DD');

import dayjs, { Dayjs } from 'dayjs'; 日期物件 
import moment from 'moment'; 將日期格式化
const dateBgn_default = moment().subtract(3, 'months'); //開始日期_預設_3個月前
const dateEnd_default = moment(); //結束日期_預設_今天

const dateFormat = "YYYY-MM-DD"

const dateRangesOptions = {
  本週: [
    dayjs(moment().startOf('week').format(dateFormat), dateFormat),
    dayjs(moment().endOf('week').format(dateFormat), dateFormat),
  ],
  上週: [
    dayjs(moment().startOf('week').subtract(1, 'weeks').format(dateFormat), dateFormat),
    dayjs(moment().endOf('week').subtract(1, 'weeks').format(dateFormat), dateFormat),
  ],
  本月: [
    dayjs(moment().startOf('month').format(dateFormat), dateFormat),
    dayjs(moment().endOf('month').format(dateFormat), dateFormat),
  ],
  上月: [
    dayjs(moment().startOf('month').subtract(1, 'months').format(dateFormat), dateFormat),
    dayjs(moment().endOf('month').subtract(1, 'months').format(dateFormat), dateFormat),
  ],
};


本週第一天
moment().startOf('week').format(dateFormat)
本週最後一天
moment().endOf('week').format(dateFormat)
本月第一天
moment().startOf('month').format(dateFormat)
本月最後一天
moment().endOf('month').format(dateFormat)

string to 物件
let t:dayjs.Dayjs = dayjs('2019-01-01 11:11:59');
物件 to string
t.format('YYYY-MM-DD hh:mm:ss')

加一天
t = t.add(1, 'days');
加一秒
t = t.add(1, "second");
